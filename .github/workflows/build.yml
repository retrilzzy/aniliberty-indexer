name: Build

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum', '**/go.mod') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Download dependencies
        run: go mod download

      - name: Verify dependencies
        run: go mod verify

      - name: Run go vet
        run: go vet ./...

      - name: Build
        run: go build -v ./...

      - name: Test
        run: go test -v ./...

      - name: Build binaries
        run: |
          mkdir -p dist
          GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -trimpath -o dist/aniliberty-indexer-linux-amd64 ./cmd/indexer
          GOOS=linux GOARCH=arm64 go build -ldflags="-s -w" -trimpath -o dist/aniliberty-indexer-linux-arm64 ./cmd/indexer
          GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -trimpath -o dist/aniliberty-indexer-darwin-amd64 ./cmd/indexer
          GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -trimpath -o dist/aniliberty-indexer-darwin-arm64 ./cmd/indexer
          GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -trimpath -o dist/aniliberty-indexer-windows-amd64.exe ./cmd/indexer

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binaries
          path: dist/
          retention-days: 7
